using Senti.Data;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;



namespace MobilePaywall.Data
{
  public partial interface IRouteSetManager
  {
    List<RouteSet> Load(Instance instance);
    List<RouteSet> Load(IConnectionInfo connection, Instance instance);
    
    RouteSet Load(Instance instance, string name);
    RouteSet Load(IConnectionInfo connection, Instance instance, string name);




  }

  public partial class RouteSet
  {
  }
}

