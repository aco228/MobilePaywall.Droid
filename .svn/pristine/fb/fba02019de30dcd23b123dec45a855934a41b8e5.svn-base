using Senti.Data;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

 

namespace MobilePaywall.Data 
{
  public partial interface IInitiateCallbackManager
  {
    InitiateCallback Load(Guid initiateCallbackGuid);
    InitiateCallback Load(IConnectionInfo connection, Guid initiateCallbackGuid);
    InitiateCallback Load(PaymentRequest paymentRequest);
    InitiateCallback Load(IConnectionInfo connection, PaymentRequest paymentRequest);
  }

  public partial class InitiateCallback
  {
  }
}

